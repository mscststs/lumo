这是一个浏览器侧边栏和 Agent 对话项目，你的目的是构建一个高效、智能的浏览器插件，该插件包含了一个基于第三方 Agent API 的模型连接能力，并实现 “所有” 在浏览器中可能需要的交互功能，使 Agent 能协助用户完成近乎所有的浏览器操作、包括数据识别、搜集、办公自动化等能力。

### 背景和技术规范

基于 WXT 开发的 Chrome Extension V3 项目，基础框架采用 React、TailwindCSS、TypeScript。
使用 ai-element + shadcn 来构建所有页面，使用 Motion 来进行动画和效果增强。

### 解决方案要求

#### 基本要求：

1. i18n 国际化：所有文本内容必须支持多语言，至少包括中文和英文。
2. 响应式设计：所有页面和组件必须适配不同尺寸，考虑到侧边栏宽度可以被手动。
    - 按钮、文本、段落需要注意宽度导致的换行与折叠，尽可能避免换行导致页面布局错乱。
3. 多主题色支持：使用标准的 shadcn 主题色方案，支持至少两种主题色（浅色和深色），并允许用户自定义主题色。

#### 产品要求：

符合  Linear-style / Craft-driven Product Philosophy 的设计哲学。
所有涉及前端展示的功能与组件，必须通过以下四项核心设计法则的审查。

##### 1. 渐进式隐藏（Invisible Assistance）

> 「真正的好功能，不需要任何解释性文本。」

- **消灭提示文本**：严禁出现 "请点击这里"、"请在此输入" 等低效文案。组件的形式、位置和状态必须具备自解释性。
- **拒绝画蛇添足**：删除所有 "点击 X 以达到 Y 目的" 的交互性废话。不把用户当傻瓜，保持页面的高信息密度与纯净度。
- **情境触发显隐**：严禁功能在不适宜的场景下堆砌。当触发前提未满足时，必须彻底隐藏该组件（例如：Sidepanel 的 SplitView 按钮，在视口宽度不足时必须直接销毁或隐藏，不留残影）。

##### 2. 交互归一化（Coherent Familiarity）

> 「交互即肌肉记忆。相似的动作，必须带来绝对一致的物理反馈。」

- **组件形态归一**：同一系统或产品内，下拉框、弹窗、气泡等基础组件的视觉样式、交互热区与弹出方向必须完全统一。
- **深层体验同构**：同一个细分功能在不同业务场景下的逻辑表现必须一致。例如「拖拽至输入框」，无论是拖拽文件、代码片段还是历史记录，在不同页面都必须维持相同的吸附动画、高亮状态和放置逻辑。

##### 3. 渐进式披露（Progressive Disclosure）

> 「掌控复杂度的秘诀，在于精细化管理用户的认知负载。」

- **状态实时共鸣**：所有包含状态、步骤、生命周期的组件，必须完美设计其中间态（Loading、Empty、Interrupt）。当后端或上下文发生变化时（如会话中断），视图（如 MCP 状态）必须无缝且实时地与物理场景同步。
- **视界视觉留白**：复杂组件必须具备极度克制的折叠与收纳机制。将次要的内部状态隐藏在折叠层下，永远将主视图的绝对重点、最开阔的视觉空间留给用户的核心操作。


##### 4. 物理级动效（Cinematic Momentum）

> 「动效不是装饰，而是视觉重心的物理位移指南。」

- **视觉惯性守恒**：动效必须符合真实的物理逻辑。从左侧消失的元素，必须推动其余元素以平滑的 transition 向左平移填补空缺，严禁出现没有任何物理前因后果的"闪现"或"突变"。
- **反视觉疲劳**：保持动效的优雅与克制。仅在重大的大面积元素显隐时使用宏观动画；高频、局部的微观交互应采用极短、极轻的物理阻尼动效，避免过度炫技导致用户视觉疲劳。


### 规范

1. 你的问题、分析与解决方案需要贴合当前的项目的架构和实现，优先在脚手架和项目内查找原因、配置和实现方法。

2. 总是依据最前沿的互联网web项目的实施方法，当有多种方案备选时，总是优先推荐抽象最合理、后期维护性更好的。

3. 你的分析、解决流程必须精准，你总是能高效发现众多差异或者问题中最关键的问题。

4. 你必须先根据用户的意图进行分析、总结，并查到关键的模块、功能和流程，然后再根据分析结果给出解决方案。你必须让用户确认方案后才可以实施，禁止直接开始编码。

### 输出

你的执行过程、分析与思考，必须使用 “简体中文” 来输出，除非是专用的名词。

### 步骤

1. 尝试解读用户意图和需求，并敏捷发掘相关的模块，如果此过程中有多个方向不确定，可以快速询问用户。

2. 获取需求后，需要根据需求和意图，结合代码结构，敏锐发现关键文件、模块、入口，并根据调用链、依赖关系、模块内实现，甚至 npm 包原始实现， 进行分析，找出问题的根源和解决方案。

3. 充分分析和研究后，可以根据你的研究方向以及当前项目的目标、实现，给出一个或多个可行的解决方案，并列出每个方案的优缺点，等待用户确认。

4. 在用户的确认或者多轮的对话以修正目标和方案后，开始依照方案进行实施，实施时需要注意，对于模块的变更、上下游的参数变化，需要同步修改前后端或调用链上的所有依赖，并确保整体的功能完整。

5. 在实施完毕后，充分测试你的修改，你的测试方案应当是全面、符合逻辑的。