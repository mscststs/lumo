这是一个浏览器侧边栏和 Agent 对话项目，你的目的是构建一个高效、智能的浏览器插件，该插件包含了一个基于第三方 Agent API 的模型连接能力，并实现 “所有” 在浏览器中可能需要的交互功能，使 Agent 能协助用户完成近乎所有的浏览器操作、包括数据识别、搜集、办公自动化等能力。

### 背景和技术规范
基于 WXT 开发的 Chrome Extension V3 项目，基础框架采用 React、TailwindCSS、TypeScript。
使用 ai-element + shadcn 来构建所有页面，使用 Motion 来进行动画和效果增强。

### 解决方案要求
你的所有解决方案必须遵循以下要求：

1. i18n 国际化：所有文本内容必须支持多语言，至少包括中文和英文。
2. 响应式设计：所有页面和组件必须适配不同尺寸，考虑到侧边栏宽度可以被手动。
    - 按钮、文本、段落需要注意宽度导致的换行与折叠，尽可能避免换行导致页面布局错乱。
3. 多主题色支持：使用标准的 shadcn 主题色方案，支持至少两种主题色（浅色和深色），并允许用户自定义主题色。
4. 代码规范：所有代码必须遵循 TypeScript 的最佳实践，使用 React Hooks。你的页面、组件都必须保持功能独立、高内聚、低耦合，尽量避免全局状态和全局变量的使用。尽量避免一个页面只有一个组件，你需要将页面的功能拆分为多个组件，保持组件的独立性和可复用性。

### 执行偏好
1. 你的问题、分析与解决方案需要贴合当前的项目的架构和实现，优先在脚手架和项目内查找原因、配置和实现方法。

2. 总是依据最前沿的互联网web项目的实施方法，当有多种方案备选时，总是优先推荐抽象最合理、后期维护性更好的。

3. 你的分析、解决流程必须精准，你总是能高效发现众多差异或者问题中最关键的问题。

### 输出
你的执行过程、分析与思考，必须使用 “简体中文” 来输出，除非是专用的名词。